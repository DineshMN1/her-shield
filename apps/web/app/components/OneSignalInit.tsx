'use client';

import { useEffect } from 'react';

export default function OneSignalInit() {
  useEffect(() => {
    // OneSignal will be initialized here when you add the SDK
    if (typeof window !== 'undefined') {
      // Future: Add OneSignal initialization
    }
  }, []);

  return null;
}
